# AI rules (PayFlow)

This folder mirrors **[`.cursor/rules/payflow.mdc`](../.cursor/rules/payflow.mdc)** so workspace settings that include `development-standards/ai-rules` load the same project rules.

Edit **one** source of truth (prefer `.cursor/rules/payflow.mdc`) and keep `payflow.mdc` here in sync when you change conventions.
