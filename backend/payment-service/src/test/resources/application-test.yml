# AWS Secrets Manager is NOT loaded for tests.
# DB credentials are provided by Testcontainers via @DynamicPropertySource.
# The aws profile must be activated explicitly (SPRING_PROFILES_ACTIVE=aws) in production.
